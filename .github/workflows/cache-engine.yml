name: Entry Engine (Stable Starter)

on:
  push:
    branches: [main]

  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Basic test run
        run: |
          echo "System is alive"
          mkdir -p output
          echo "OK" > output/status.txt
